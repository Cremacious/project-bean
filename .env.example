# .env.example
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# BetterAuth. Generate the secret with: openssl rand -base64 32
BETTER_AUTH_SECRET="generate-with: openssl rand -base64 32"
BETTER_AUTH_URL="http://localhost:3000"

# Public base URL used by the browser auth client.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional social providers (leave unset in dev). When both a provider's
# CLIENT_ID and CLIENT_SECRET are present, that provider is enabled.
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# APPLE_CLIENT_ID=""
# APPLE_CLIENT_SECRET=""
